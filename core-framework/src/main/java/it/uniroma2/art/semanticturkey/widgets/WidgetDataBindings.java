package it.uniroma2.art.semanticturkey.widgets;

public enum WidgetDataBindings {

    //for maps data
    route_id,
    location,
    latitude,
    longitude,
    //for charts data
    series_collection_id,
    series_id,
    series_name,
    name,
    value,
    x_axis_label,
    y_axis_label,
}
