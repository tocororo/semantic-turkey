package it.uniroma2.art.semanticturkey.config.customview;

public enum CustomViewDataBindings {

    //for geospatial
    route_id,
    location,
    latitude,
    longitude,
    //for statistical series
    series_collection_id,
    series_id,
    series_name,
    name,
    value,
    value_label,
    series_label,

}
